import "../globals.css";
import { ReactNode } from "react";

export const metadata = {
  title: "Orián — What do you want to build?",
  description: "Describe your project idea and get a structured brief in minutes.",
};

export default function ScopingLayout({ children }: { children: ReactNode }) {
  return children;
}
